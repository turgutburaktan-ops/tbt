rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function signedIn() {
      return request.auth != null;
    }

    // Public content can be browsed without signing in.
    match /{document=**} {
      allow read: if true;
      allow write: if signedIn();
    }
  }
}
