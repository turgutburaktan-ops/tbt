name: Iris Fixed Android APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.5'
          channel: stable
          cache: true

      - name: Recreate Android project
        run: rm -rf android && flutter create --platforms=android .

      - name: Prepare Android build
        env:
          MAPS_API_KEY: ${{ secrets.MAPS_API_KEY }}
        run: python3 tool/prepare_android_build.py

      - name: Install dependencies
        run: flutter pub get

      - name: Patch fixed camera layout
        run: python3 tool/patch_camera_layout.py

      - name: Patch Iris Android capture
        run: python3 tool/patch_iris_camera.py

      - name: Patch live manual exposure
        run: python3 tool/patch_live_manual_exposure.py

      - name: Patch AI camera behavior
        run: python3 tool/patch_ai_camera_behavior.py

      - name: Patch cinematic realism
        run: python3 tool/patch_cinematic_realism.py

      - name: Patch live AI parameter HUD
        run: python3 tool/patch_live_ai_hud.py

      - name: Patch messages navigation
        run: python3 tool/patch_messages_navigation.py

      - name: Patch camera exposure and responsive layout V2
        run: python3 tool/patch_camera_exposure_layout_v2.py

      - name: Patch mode-aware AI camera profiles V2
        run: python3 tool/patch_mode_aware_ai_camera_v2.py

      - name: Analyze
        run: |
          rm -rf test
          flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: Build release APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: en-iyi-cekim-noktasi-ai-camera-apk
          path: build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error
          retention-days: 7
