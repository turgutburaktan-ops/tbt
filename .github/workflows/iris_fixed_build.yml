name: Iris Fixed Android APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Recreate Android project
        run: rm -rf android && flutter create --platforms=android .

      - name: Prepare Android build
        env:
          MAPS_API_KEY: ${{ secrets.MAPS_API_KEY }}
        run: python3 tool/prepare_android_build.py

      - name: Guard exposure during tap focus
        run: python3 tool/patch_focus_exposure_guard.py

      - name: Install dependencies
        run: flutter pub get

      - name: Patch Iris Android capture
        run: python3 tool/patch_iris_camera.py

      - name: Enable physical 0.5x and tele lenses
        run: python3 tool/patch_iris_physical_lenses.py

      - name: Add exact lens ID switching
        run: python3 tool/patch_iris_direct_lens_id.py

      - name: Route camera buttons to exact lens IDs
        run: python3 tool/patch_camera_direct_lens_switch.py

      - name: Map zoom buttons by real FOV
        run: python3 tool/patch_camera_fov_lens_mapping.py

      - name: Apply camera runtime fidelity fixes
        run: python3 tool/patch_camera_runtime_fidelity.py

      - name: Optimize GPU editor preview
        run: python3 tool/patch_gpu_editor_runtime.py

      - name: Add event city autocomplete
        run: python3 tool/patch_social_event_city_autocomplete.py

      - name: Analyze
        continue-on-error: true
        run: |
          rm -rf test
          mkdir -p build/diagnostics
          flutter analyze --no-fatal-infos --no-fatal-warnings 2>&1 | tee build/diagnostics/flutter_analyze.log

      - name: Upload analyzer diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: flutter-analyze-diagnostics
          path: build/diagnostics/flutter_analyze.log
          if-no-files-found: warn
          retention-days: 7

      - name: Build release APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: en-iyi-cekim-noktasi-pro-camera-apk
          path: build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error
          retention-days: 7
